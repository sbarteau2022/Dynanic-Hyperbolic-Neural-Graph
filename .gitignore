node_modules/
dist/
*.log
.DS_Store

# device-local: real recall events and published atlas snapshots never get
# committed — they're this machine's memory, not source code.
atlas/
data/events.json
data/cursor.json
